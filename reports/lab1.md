# lab3 实验报告

## 编程作业
1. 在TaskControlBlock中，添加一个新的字段，类型为[isize; 5]，用于记录当前任务各个系统调用的次数。
2. 拓展TaskManager，新增两个方法：
    1. `fn get_syscall_counter(&self, syscall_id_transformed: usize) -> isize`：返回当前任务对应系统调用的总次数。
    2. `fn set_syscall_counter(&self, syscall_id_transformed: usize) `：当当前任务调用某个系统调用时对该记录+1。
3. 根据要求实现sys_trace   
    如果 `trace_request` 为 0，读取当前任务 `id` 地址处一个字节的无符号整数值。返回值为 `id` 地址处的值。
    如果 `trace_request` 为 1，则写入 `data` 到该用户程序 `id` 地址处。返回值应为0。
    如果 `trace_request` 为 2，查询当前任务调用编号为 `id` 的系统调用的次数，返回值为这个调用次数。
    否则，忽略其他参数，返回值为 -1。


## 简答作业
1. 正确进入 U 态后，程序的特征还应有：使用 S 态特权指令，访问 S 态寄存器后会报错。 请同学们可以自行测试这些内容（运行 三个 bad 测例 (ch2b_bad_*.rs) ）， 描述程序出错行为，同时注意注明你使用的 sbi 及其版本。

    > 答：由于在U态尝试使用S态的指令，或尝试访问S态的寄存器，会产生非法指令错误的异常，进而被位于S态内核捕获并处理。   
    SBI版本：RustSBI version 0.3.0-alpha.2, adapting to RISC-V SBI v1.0.0

2. 深入理解 trap.S 中两个函数 __alltraps 和 __restore 的作用，并回答如下问题

    1. L40：刚进入 __restore 时，sp 代表了什么值。请指出 __restore 的两种使用情景。
        > 答：  
        sp 指向内核栈的栈顶.   
        使用场景：1. 初始化用户态上下文；2. trap后恢复保存的用户态上下文；

    2. L43-L48：这几行汇编代码特殊处理了哪些寄存器？这些寄存器的的值对于进入用户态有何意义？请分别解释。
        ```
        ld t0, 32*8(sp)
        ld t1, 33*8(sp)
        ld t2, 2*8(sp)
        csrw sstatus, t0
        csrw sepc, t1
        csrw sscratch, t2
        ```
        > 答：  
        status 记录 Trap 前的特权级（SPP 字段）、中断状态等，决定返回后的特权级和执行环境  
        sepc 记录返回后应执行的指令地址  
        sp 保存用户态栈顶地址，用于恢复用户栈指针
    3. L50-L56：为何跳过了 x2 和 x4？
        ```
        ld x1, 1*8(sp)
        ld x3, 3*8(sp)
        .set n, 5
        .rept 27
        LOAD_GP %n
        .set n, n+1
        .endr
        ```
        > 答：  
        x2 是用户态栈指针，单独通过 sscratch 管理  
        x4 按照约定用于传递第二个函数参数，属于调用者保存寄存器，这里没用到
    4. L60：该指令之后，sp 和 sscratch 中的值分别有什么意义？
        ```
        csrrw sp, sscratch, sp
        ```
        > 答：将寄存器 sp 的当前值写入控制寄存器 sscratch，同时将 sscratch 原来的值读取到寄存器 sp 中。此时sp指向用户栈，sscratch指向了内核栈。
    5. __restore：中发生状态切换在哪一条指令？为何该指令执行之后会进入用户态？
        > 答：状态切换发生在最后一条指令 sret。RISC-V 规定，sret 指令会将当前特权级别切换为 SPP 记录的值

    6. L13：该指令之后，sp 和 sscratch 中的值分别有什么意义？
        ```
        csrrw sp, sscratch, sp
        ```
        > 答：sp指向内核栈，sscratch指向用户栈
    7. 从 U 态进入 S 态是哪一条指令发生的？
        > 答：sret


## 荣誉准则
1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与以下各位就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

     > 无

2. 此外，我也参考了以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

     > rCore-Tutorial-Book-v3 3.6.0-alpha.1 文档  
     rCore-Tutorial-Guide-2025S  文档

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。